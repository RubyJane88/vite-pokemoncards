import styled from "@emotion/styled";

export const Card = styled("div")`
  max-width: 35rem;
  margin: 3rem 2rem 3rem 2rem;
  padding: 1px;
  background-color: rgba(28, 27, 27, 0.07);
  border-radius: 8px;
  font-family: "AppleGothic";
  font-weight: lighter;
`;
